{
  "name": "forward",
  "version": "0.0.1",
  "description": "",
  "main": "./build/forward.ts",
  "types": "./build/forward.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b ./src/tsconfig.json"
  },
  "author": "Zim",
  "dependencies": {
    "@types/node": "^16.4.10",
    "sorted-queue": "^0.3.2"
  }
}
